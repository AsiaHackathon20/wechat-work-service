#spring:
#  profiles: dev

server:
  port: 9090

wechatwork:
  corpid:test_corp_id
  corpsecret:test_corp_secret
  externalcontact:test_external_contact
    token:test_token
    aesKey:test_aesKey

logging:
  config: 'classpath:logback.xml'

gateways:
  policyGateway:
    baseurl: http://localhost:8080
